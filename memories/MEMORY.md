User works on Hermes_Skills repo on Windows + aspirecures repo (weekly research feed pipeline with two-agent cronjob architecture). Always triple-checks: git sync, stray dirs, orphaned branches, audit thresholds, env parity. Prefers perfect/foolproof autonomous solutions. Triple-check expectation: after deliverables verify (1) git fully pushed & 0 ahead/behind, (2) working tree clean, (3) no temp files, (4) no accidental deletions, (5) audit passes.
§
State after 2026-09-06 two-pass formatting/organization: HEAD 1675aa6 pushed. Pass 1 (8c19c0c): README catalog live-regenerated, all 23 category DESCRIPTIONs filled, phantom mlops/models/ deleted repo+local, cron validator scripts tracked via .gitignore un-ignore. Pass 2 (1675aa6): CODE-INDEX.md + tools/gen-code-index.py = flat index of every script/helper/test/template with purpose from docstring (regen after code changes); orphaned code documented in owning SKILLs; gh-env.sh elif side-effect assignment normalized (both copies, verified live); byte-identical validate-cronjobs.py orphan under cron-config-authoring/scripts/ removed repo+local (.hermes/cron/ = single source of truth). Local has 2 extra hub skills not in repo: research/rss-feeds, social-media/reddit-reading.
§
aspirecures-weekly-research cron (Mon 9:17 AM ET, two-agent pipeline, 9 disease pages): DISABLED as of 2026-09-06; prompt's LM Studio health check still targets stale port 1234 + qwen/qwen3.6-35b-a3b — update to 42069 before re-enabling.
§
Profile MEMORY.md key learnings: 59-char description limit (index truncates >57 chars), YAML frontmatter rules, mattpocock ownership conventions.
§
Hermes config (verified 2026-09-06 from config.yaml): default model qwen3.8-27b@q4_k_xl via LMStudio at http://127.0.0.1:42069/v1; profiles the-memory-controller (poolside laguna-s-2.1) + the-skill-maker (opencode zen).
§
Profile structure: PROFILE.md, USER.md, MEMORY.md, config.yaml, .usage.json (per-skill metrics), .curator_ledger.jsonl (audit trail with sha256). 5 bot platforms at profile level: telegram/hermes-telegram, discord/hermes-discord, whatsapp/hermes-whatsapp, slack/hermes-slack, signal/hermes-signal.
§
economicspace stamps verified from module code 2026-09-07: catalog 1.2.0 / mineral_value 1.9.0 / transportation 1.14.0 / calc 1.20.0 (charge_insurance=False default — ALL committed measurements incl. the 20-cell campaign are charge_insurance=True runs; set True to reproduce any) / master 1.24.0. Local build_master.py had an uncommitted revert of both literals to 1.23.0 = OneDrive churn mid-sync (master.py on disk still 1.24.0); verify git status before trusting either copy. calc 1.20.0: flag ON is bit-identical to 1.19.1; winners unchanged; columns stay 141 with the two insurance fields zeroed.
§
economicspace library landscape (from user's star list loggger101/lists/for-econ-space-pipeline, 17 repos): none imported yet. Candidates to wire in: brahe (MIT, pip-installable, JOSS-reviewed) = clean dv cross-check top pick; skyfield (MIT, numpy-only) = ephemerides/phase-angle validation; astroquery (BSD-3) = catalog enrichment; pymc (Apache-2.0) = optional Bayesian uncertainty. CORRECTIONS from round 2: nyx-py IS live (pip install nyx_space, py>=3.11, real monte_carlo module MvnSpacecraft/StateDispersion); CamPyRoS runs without ray via ray_alt.py serial shim on Windows. Remaining blockers: pds4_tools (no license detected), space-map (AGPL tiny). Full table in skill orbital-mechanics-data/references/economicspace-library-landscape.md; dedicated skill economicspace-pipeline.
§
Space-mission computation paradigms (from for-econ-space-pipeline list, 5 distinct non-interchangeable methods): (1) closed-form patched conics = economicspace calc.py baseline, instant+bit-reproducible, RANK targets; (2) numerical propagation RK4/SGP4 + DE ephemerides (brahe/skyfield) = VERIFY a dv number / exact phase angles; (3) successive convexification OpenSCvx JAX+CVXPY (free final time, adaptive time dilation s appended to control vector, continuous-time constraint satisfaction arXiv 2404.16826, FOH/ZOH discretization, AOT multishooting) = DESIGN a specific maneuver's thrust profile under hard constraints; (4) parallel global multiobjective pygmo/pagmo island model CMA-ES/DE/PSO = robust optimum across uncertainty / Pareto fronts (PyPI wheels Linux-only); (5) 6DOF forward-integration Monte Carlo CamPyRoS + aeroheating = launch dispersion, atmospheric not interplanetary. Rule: RANK closed-form, VERIFY propagation, DESIGN convexification, OPTIMIZE global, DISPERSION 6DOF-MC. Full guide in skill space-mission-computation-paradigms.
§
brahe (duncaneddy/brahe) Python bindings live in crates/brahe-py/ (NOT top-level brahe-py — round-2 note was wrong): propagators+integrators(RK4/SGP4), orbit determination, frames/time/EOP, ICGEM Earth gravity models, relative motion, access windows AND data clients for SBDB/JPL Horizons/SPICE/Celestrak/Space-Track. One MIT lib overlaps BOTH economicspace stage 1 (catalog sources) and stage 4 (dv math); ships comparative benchmarks vs nyx -> fair cross-check target.
§
pygame/pygame surveyed 2026-09-04 -> skill creative/pygame (headless SDL_VIDEODRIVER=dummy before import; mixer.pre_init before pg.init(); assets after set_mode for .convert(); Sprite lives only while a Group holds it; dirty rects via RenderUpdates; test runner: subprocess isolation OPT-IN -s, 120 s kill timer default, 'untrusty' re-run of failed modules).
§
Session-Knowledge-Report docx delivered in chat only 2026-09-04; temp copy %LOCALAPPDATA%\Temp\econ-report deleted same day. economicspace-pipeline SKILL.md 'Open candidates' section = 14 items (refreshed round-4, 2026-09-07) + orbital-mechanics-data landscape ref has per-candidate next-steps table ("Concrete next steps per candidate", line ~175).
§
Hermes skills dir C:\Users\Owner\AppData\Local\hermes\skills is a PLAIN directory, not a git repo -- skill edits need no commit/push; triple-check for them = file written + loads via skill_view. (Distinct from the Hermes_Skills GitHub repo.)
§
Starred-repos deep-dive COMPLETE 2026-09-05: all 41 stars covered read-only; ALL 6 cluster reports on disk in %LOCALAPPDATA%\hermes\output\starred-dive\ (FINDINGS-verified.md, scicomp-nasa.md, space-data-viz.md + salvaged data-engineering.md, webdev.md, creative-design.md from dead subagent transcripts). Skills ported: data-science/space-data-pipelines, data-science/astro-toolkit-selection, creative/diagram-design (212 files), creative/design-taste-frontend, creative/redesign-existing-projects, creative/full-output-enforcement, software-development/grilling-interview + wayfinder-map-planning + conversation-to-spec, github/issue-triage-state-machine (+AGENT-BRIEF & OUT-OF-SCOPE refs), frontend-design/nicegui-app-builder. New verified facts: Front-End-Checklist = monorepo WITH MCP server package (agent-consumable QA rules); NiceGUI ui.run() = 33 named params (verified from source in round-2 pass; earlier '71' and '30+' both wrong) incl. native=True pywebview + storage_secret for encrypted app.storage.user, 59 example apps; polars lazy-first w/ join validate= cardinality checks; pymc sample() sampling/mcmc.py:620 + nutpie Rust NUTS auto-selected when installed. Ops lesson: deep-research subagents on qwen3.8-27b@q4 die at ~9h context limit — instruct them to append report files per repo so partial work survives; 5 of 7 died but transcripts (cache/delegation/live/<id>/task-N.log) were fully salvageable.
§
Hermes_Skills conventions (enforced, all thresholds zero): pre-commit gate = `py tools/verify-all.py` (9 gates: audit, check-links, index drift x4, cron validators x2, doc-count consistency; supersedes the old audit+links pair). Use `py` on Windows -- bare `python`/`python3` are Microsoft Store alias stubs that exit 49 without running anything, and `shutil.which` returns them. Frontmatter: name/version/author/platforms/metadata.hermes; description ≤59 chars double-quoted; body needs '## What This Skill Does' + '## When to Use'; related_skills must resolve (refs to uninstalled optional skills break the gate — clean them). check-links skips URLs/code spans/profiles-export snapshots AND <skill>/templates/ dirs (fill-in-the-blank scaffolds, added 2026-09-05 round 2). Lookup ladder: DESCRIPTION.md Task→Skill table → grep SKILLS-INDEX.md → skill → related_skills → DEPENDENCY.md. Regen tools stdlib-only: gen-skills-index.py, regen-dependency-map.py (standalone — sync script's built-in generator hangs on import), check-links.py. Weekly cronjobs are DEFINED but NOT REGISTERED (verified 2026-09-08 against %LOCALAPPDATA%/hermes/cron/jobs.json + executions.db): the live scheduler holds exactly ONE job, aspirecures-weekly-research, and it is disabled, with one execution ever (2026-08-31). skill-audit (Sun 3AM) and sync-hermes-skills (Sun 2AM) exist only as JSON definitions under .hermes/cron/active/ and have never run -- so nothing audits or syncs this repo unattended; run `py tools/verify-all.py` yourself. Also live-vs-repo drift on the one registered job: live workdir is Hermes_Skills but the repo config says the aspirecures repo. Commit author for automation: hermes-cronbot <cronbot@hermes.local>. Pitfall: git index can corrupt with empty blobs staged as deletions while identical content sits on disk (OneDrive churn) — verify `git show HEAD:f` vs disk hash before reset; plain `git reset` repairs losslessly. OPEN ITEM: root AGENTS.md drafted but write gated behind user approval — ask before creating it.
§
Hermes skill hub (big find 2026-09-05): the hermes-agent repo ships optional-skills/ = first-party skills installable via `hermes skills install official/<cat>/<name> --yes` (--yes skips confirm; inspect with `hermes skills inspect`). Hub-installed now 15 (lock.json verified 2026-09-07): qmd, scrapling, code-wiki, rest-graphql-debug, ast-grep, watchers, fastmcp, har-derived-api-client, jupyter-notebook, one-three-one-rule, decision-questionnaire, oss-forensics + huggingface-trackio, parallel-cli, adversarial-ux-test. Hub-installed skills follow upstream authoring standard (no '## What This Skill Does' section) — when syncing to Hermes_Skills repo, add real summary sections or the audit gate fails; also clean related_skills refs pointing at uninstalled optional skills. Round 2 external ports in local+repo: code-quality-signal (sentrux), duckdb-querying, modern-python-tooling / property-based-testing / semgrep-rule-creator (trailofbits — CC-BY-SA-4.0). VoltAgent/awesome-agent-skills README badge claims 1497+ skills and grows over time; trailofbits/skills + duckdb/duckdb-skills are public GitHub repos behind the top entries.
§
Second-brain maintenance protocol (proven 2026-09-05): full inventory diff = every SKILL.md by frontmatter name + CRLF-normalized bytes, local $LOCALAPPDATA/hermes/skills <-> repo, skip profile/ and profiles-export/. Sync direction is PER-SKILL — always diff first; the repo copy can be ahead of local (python-craft had 2 Windows-gotcha entries only in repo). Never overwrite files under profiles-export/ with live content — they are historical snapshots. OneDrive cache lag causes false parity mismatches right after writes: sleep ~0.3s and re-verify before concluding drift. After any skill change: regen SKILLS-INDEX + DEPENDENCY, run audit+check-links pair, commit (hermes-cronbot), push, verify remote HEAD via gh api + 0 ahead/behind.
§
Verified API facts (round-2 pass, now in owning skills' references/ after the 2026-09-07 org pass): nicegui ui.run() = exactly 33 named params (verified from ui_run.py — earlier "71" and "30+" both wrong) incl. native=True pywebview + storage_secret for encrypted app.storage.user, 59 example apps; cumulus ships pvl PDS-label parser + cmr-client NASA CMR API.
§
mattpocock/skills port audit (2026-09-06): repo has 37 skills; PORTED round 3: ask-matt->skill-flow-router, teach (+4 refs), wizard (+template.sh), retro — all in local+repo at 4fe8077. to-questionnaire was already ported earlier as decision-questionnaire. Remaining unported (low value here): writing-trio shape/beats/fragments, setup-ts-deep-modules, git-guardrails-claude-code; SKIP list: implement/implement-spec/claude-handoff/grill-with-docs/wait-what/loop-me/scaffold-exercises/migrate-to-shoehorn/setup-* (thin or Claude-Code-specific). Round-1 name mappings: triage->issue-triage-state-machine, grill-me->grilling-interview, wayfinder->wayfinder-map-planning, to-spec->conversation-to-spec.
§
Tool gotcha (memory tool): 'replace'/'remove' REQUIRE a 'content' field with the full replacement text — passing only new_text fails "content is required"; old_text must be a short unique substring of ONE entry, never spanning multiple entries. CRITICAL: replace swaps the ENTIRE identified entry for content — it does NOT do substring splicing inside an entry (2026-09-09: passing a fragment as old_text + shorter snippet as content silently wiped a whole consolidated history entry; restored by re-passing full text). Always pass the complete updated entry in content. Large multi-op batches lose fields in transit and fail all-or-nothing; send small single-op calls instead — those succeed reliably.
§
gods-eye-view = Cesium app with full test coverage across all modules (architecture reference from the 41-repo dive). Round-3 backlog: port taste-skill's stitch-skill (generates agent-friendly DESIGN.md files, complements design-md) if user wants more creative tooling.
§
Starred-repos round-4 pass COMPLETE 2026-09-07 (manual, no subagents): all 41 clones re-read at source level; Hermes_Skills b2c5179 pushed & verified. Then ORG PASS same day -> b6e7b68: flat docs/ knowledge layer dissolved — every reference doc now lives in its OWNING skill's references/ (astro-toolkit-selection carries brahe/skyfield/openscvx/catalog-data-sources/optimization-toolkit; economicspace-pipeline carries dv-oracles-and-economics-sources + 3 older refs; python-data-science polars-pymc-api-reference; nicegui-app-builder frontend-tooling; github-pr-workflow git-workflow-recipes). docs/ = pointer index (README) + archive only. Current HEAD after 2026-09-07 sync: edfa21e (python-craft ref surfaced as real relative link); local skills dir in full parity with repo except by-design local-only items.
§
Round-4 LIVE findings (2026-09-07, re-probed 2026-09-08 — Asterank item CORRECTED): JPL NHATS (~7,033 NEAs) is the only POPULATION-scale external Δv oracle; Asterank's `dv` column was NOT removed repo-wide (round-4 conclusion disproven on re-probe: bulk-scan projection omits it entirely, but targeted JSON queries — {"name":"Eros"} → dv=6.112354, Apophis 5.687675, Icarus 15.30, Itokawa 9.83, 2010 PS66 15.30 — still return real Shoemaker-Helin values; query keys are name/pdes/des/number, plain-string queries HTTP-500). Asterank adds per-element sigma diagonal (σa/σe/σi... for ~600K bodies, zero extra calls) + obs-provenance keys. brahe 1.7.0 ships cp311 win_amd64 wheel → installs on this box with no Rust/CSPICE build; native SPICE reader covers type-21 kernels (Horizons small-body segments). NEODyS full 6×6 COV verified in source. OpenSCvx examples/spacecraft/ exists on live main (second-pass 'no transfer example' claim disproven). Full detail: data-science/economicspace-pipeline/references/dv-oracles-and-economics-sources.md + astro-toolkit-selection refs; runnable probes now at economicspace-pipeline/scripts/{nhats_rank_crosscheck,asterank_sigma_probe}.py (both live-tested end-to-end this pass — NHATS fetch 7,045 rows OK on this box).
§
Sync 2026-09-07 (repo→local skills dir): repo was ahead on exactly 3 content items, all applied — python-craft gained references/windows-path-separator-trap.md (+ SKILL.md link fixed to real relative markdown link so skill_view surfaces it; commit edfa21e pushed & verified via gh api); huggingface-trackio/retrieving_metrics.md stale-link fix (docs/source/*.md paths replaced with published trackio docs URL); frontend-design nested layout matched (frontend-design/frontend-design/SKILL.md, bytes identical to old flat copy). Pitfall: when copying repo→local skills dir use the FULL relative path — a bare `mkdir -p $L/<skill>` creates a stray top-level skill dir that silently breaks linked_files surfacing (happened this pass; cleaned up). Local-only by design: hub infra (.hub/.curator_*/.usage.json), rss-feeds + reddit-reading, web/DESCRIPTION.md stub.
§
Round-5 format+code-knowledge pass COMPLETE -> Hermes_Skills HEAD 8a3c3f2 pushed & verified live 2026-09-07/08 (0 ahead/behind, tree clean; aeb73d3 = round-5 proper, 8a3c3f2 = follow-up adding python-data-science/references/big-data-patterns-verify.py — re-runnable self-contained fixture harness for the big-data doc + CODE-INDEX update). Changes: (1) root DESCRIPTION.md had 23 malformed category links ([text(./cat//SKILL.md pattern — unbalanced brackets so check-links never caught them; fixed to [cat/](./cat/DESCRIPTION.md)); (2) ROOT CAUSE fix: per-category DESCRIPTION.md files claimed 'Regenerated from live frontmatter' but nothing wrote them — gen-skills-index.py now actually regenerates all 23 (handles flat, nested mlops-style subdirs, AND top-level single-skill cats like huggingface-trackio/doc-coauthoring where SKILL.md sits at <cat>/SKILL.md → link ./SKILL.md); verified 167/167 targets exist; memories/DESCRIPTION.md created. (3) New tools/gen-references-index.py -> REFERENCES-INDEX.md = flat grep index of all references/ docs, wired into DESCRIPTION lookup ladder. (4) NEW SKILL web-development/static-site-patterns (#167): curated from Front-End-Checklist's 390 agent-ready skill folders for framework-free static sites — PWA manifest/service worker/offline fallback/responsive images/Core Web Vitals; fits the vanilla no-framework website repo. (5) NEW live-tested scripts: economicspace-pipeline/scripts/nhats_rank_crosscheck.py + asterank_sigma_probe.py (both e2e verified vs live APIs); space-data-pipelines/scripts/pipeline_skeleton.py (shared infra, live 7,045 rows / 0 warnings). (6) python-data-science/references/big-data-patterns.md — duckdb ad-hoc+window SQL from CSV/parquet, polars lazy validate= joins (tokens are m:m/m:1/1:m/1:1 — 'n:1' raises), parquet zstd row-group control, incremental dedup; ALL executed in isolated venv %LOCALAPPDATA%\Temp\star-scan2 (duckdb 1.5.5/polars 1.44.1/pyarrow 25.0.1 — NOT in default env) on 200k-row fixtures, cross-checked vs pandas bit-for-bit; re-run any time via big-data-patterns-verify.py. analysis-tools-dev + data-engineer-handbook/awesome-bigdata surveyed: no unique code beyond that (documented as such).
§
Tooling-hardening pass COMPLETE 2026-09-08 -> Hermes_Skills HEAD 27e00df (PR #1 merged, 0 ahead/behind, tree clean, all 9 gates pass). ROOT PROBLEM found: audit-skills.py could report a clean pass while validating NOTHING -- `import yaml` sat inside the per-skill try/except Exception, so with pyyaml missing every skill fell to the name-only fallback: exit 0, all counters 0, skill_count 0. Verified by blocking yaml with a meta_path hook (must use find_spec, NOT the pre-3.12 find_module API -- a find_module blocker silently blocks nothing on py3.13). Fixes: yaml hoisted to module scope behind a fatal guard in audit-skills/regen-dependency-map/sync-hermes-skills; env guard on all 10 tools (py3.8+ floor, UTF-8 stdout -- a cp1252 console used to kill validate-cronjobs.py on its first checkmark); audit breaches when skill_count<100; check-links treats an unreadable file as FATAL (a transient OneDrive lock silently cut 23 links from coverage and still printed 'no broken links'); generators refuse to overwrite an index from an empty scan and gained --check drift mode via new tools/_index_output.py. sync-hermes-skills.py: interpreter is sys.executable (which('python') returns the Store stub); unparseable audit output = FAILED not threshold_breached=false; commit/push now GATED on the audit (it used to push regardless and exit 1 afterwards, too late); delete phase capped at MAX_DELETIONS=25 (--allow-mass-delete override) so a half-populated local tree cannot erase and push away every skill; git stash pop failures surface instead of stranding work. NEW tools/verify-all.py = the one command. Also fixed: .gitignore `.hermes/cron/*` silently swallowed every NEW cron config/archive/template (existing ones survived only by being tracked); gen-references-index globbed references/*.md and hid 42 nested docs (254->296); counts reconciled to 167 everywhere; requirements.txt declares pyyaml (it listed only jupyterlab). MEASUREMENT LESSON: never read $? after a pipe or after a $(...) substitution -- both report the wrong command's status; I mis-reported the Store stub as 'exit 0' twice before measuring bare.
§
CR-pipeline FULL HISTORY + STATE (as of 2026-09-09; supersedes all earlier CR entries). Repo C:/Users/Owner/OneDrive/Documents/GitHub/CR-pipeline, remote loggger101/CR-pipeline. Env = system Python313 (C:\Users\Owner\AppData\Local\Programs\Python\Python313) -- NOT hermes venv; full suite ~9-10 min uncontended -> run in background. Baseline bcfa192 (513 passed): multi-layer PolicySpec 66 features, hidden_dims configurable via TrainingConfig.hidden_layers / `crp train --hidden-layers W...`, pygame viewer + crp watch. Pass A genetics = 8c16e4c: root cause was raw-score softmax selection x0.1 (eff temp ~0.001 -> de-facto argmax -> clone collapse); fixes z-scored tempered softmax, champion_refinements=2 default, adaptive mutation std (x2 after ga_stagnation_window flat gens, bounded), immigration guard (<25% diversity). Pass B net = 1b33dda: default 66->96->72->56->40->7 = 20,071 params; MEASURED finding mutation_rate is per-weight probability (expected mutations/child = rate x genome size) -- doubling params pushed ~1381->~3011 and made selection WORSE -> max_expected_mutations=1400 cap via scale_mutation_rate() wired through EvolutionConfig/TrainingConfig/YAML/CLI, no-op for small configs. Pass C viewer/docs = d9cd9fe: seeded replay (--seed N replays the EXACT game), real winner overlay (step-result termination info; old code peeked nonexistent eng._last_result_info so no winner ever showed), overtime status, honest older-shape checkpoint errors, pygame>=2.5.0 added to requirements.txt AND environment.yml, all 4 examples rewritten on verified live API and executed end-to-end, SESSION_SUMMARY v0.4, pyproject 0.4.0. Pass D rules = c617acd: WorkerConfig drift (overtime_ticks 120->600, elixir_regen_rate ~8x real -> None) + _run_head_to_head's own stale 'else 120' OT fallback aligned to engine sudden-death; both recalibrated guards RE-MEASURED under new rules before shipping. Round 10 = 7c09d72 (finished off an early-ended session) + docs pass e9fd8ee = CURRENT HEAD: match_duration was dead config in BOTH eval paths (tournament path had no duration handling at all) -> shared EvolutionTrainer._sim_overrides(): explicit --sim-config wins over named preset (full=1800/short=600/overtime=2400 ticks); plain 'full' passes NO override = bit-identical to engine defaults; flows through run_tournament into all 5 formats via ParallelRunner.run_pairings; end-to-end regression test; suite 551->554 passed / 0 failed. Docs pass: README gained a Match-rules subsection + Train-tab match-length note; SESSION_SUMMARY records the longrun outcome and updated 'Where to pick up'; fixed a false claim in a test docstring (there is NO --match-duration CLI flag — preset comes from TrainingConfig/UI, per-field overrides from --sim-config). Recalibrated guards (provenance comments in tests): ranking_tracks_skill_not_noise threshold >-0.1; champion_beats_ancestor one-sided binomial alpha=0.10 flags <=6W/20; selection_raises_mean_fitness = paired evo-vs-drift control, matches=6 + 5-gen window, gap >= pooled SE. Longrun_20260909_0041 (pop 64 swiss full-length): clean early stop gen 55/250, best ELO 1631.08 @ gen 26 agent_58, ~38 s/gen; left as-is per user ('ignore the goal running'), NOT resumed; runs dir = gitignored data. PITFALLS: _ensure_compiled re-imports DEFAULT_POLICY_SPEC fresh per call (monkeypatching parallel_runner's global alone insufficient); Population.initialize interleaves genome+agent-seed RNG draws -- replicate exactly or probe numbers are garbage; Windows multiprocessing spawn CANNOT re-import <stdin> as __main__ -- verify worker-pool code via a REAL FILE, never heredoc/stdin (caused a 14GB output flood once); pytest.approx on wrong side of comparison raises TypeError; tournament_rankings sorted by rank misaligns with agent indices -- rebuild via id map.
§
Hermes_Skills state after 2026-09-09 round-6 pass (COMPLETE): HEAD 102557e pushed, tree clean, CI green (run 34407682110). .github/workflows/ci.yml = verify-all + skill-tests; skill-tests invokes tools/run-skill-tests.py (discovery-based pytest runner — finds every <skill>/tests/ suite at runtime; --list dry-run). test-requirements.txt = single source of truth for deps. FINAL: 184 skills / 23 categories / 425 xrefs / 299 reference docs (was 167 skills at pass start). Ports this pass: superpowers {brainstorming, receiving-code-review, verification-before-completion} + ECC {regex-vs-llm-structured-text (+stdlib hybrid_parser.py, 14 tests), literature-review, windows-desktop-e2e (pywinauto/UIA), generating-python-installer (Nuitka+Inno Setup)} + research trio {pubmed-database, gget, scholar-evaluation} + round-6c {note-taking/knowledge-ops, software-development/codebase-onboarding (CLAUDE.md->AGENTS.md adaptation), software-development/living-docs-governance} + round-6d {research/bioinformatics = upstream GATEWAY indexing GPTomics/bioSkills (1,199★ MIT) + ClawBio/ClawBio (1,129★ permissive); both verified live+licensed before porting} + round-6e {devops/pinggy-tunnel (zero-install SSH tunnels w/ webhook/MCP/LLM recipes), mcp/mcporter (npx MCP client -> fastmcp)} + round-6f {web-development/publish-site (versioned Pages deploys, tag-before-deploy, live curl verification)}. Knowledge layer: hermes-agent-skill-authoring/references/skill-evolution-pipeline.md (DSPy+GEPA CLI verified from source; LiteLLM names so local LM Studio possible) + github/github-issues/references/untrusted-repo-content.md (gh-output prompt-injection threat model + stale policy, distilled from ECC github-ops). DESCRIPTION Task->Skill table COMPLETED (was missing all round-6b/c additions); category focus lines updated. BOTH source catalogs now swept to exhaustion: upstream optional-skills 140 -> 32 in brain (rest documented niche; re-scan only on new stars/needs) + ECC keyword-scored sweep of remaining 271 done, top scorers rejected with reasons (git-workflow/dmux overlap github cluster; django/flutter not used here). All 6 new stars mined: unsloth, public-apis, hermes-agent main repo (optional-skills), github-cheat-sheet (was already distilled into git-workflow-recipes.md earlier — verified); parity CLEAN by full inventory diff (0 byte mismatches common dirs; local-only = rss-feeds + reddit-reading + gitignored caches). Rejected w/ reasons in audit notes: cost-tracking, benchmark-methodology, search-first/safety-guard, mle-workflow (~90% dead cross-refs), dspy (needs hosted API keys — brain runs LM Studio), darwinian-evolver (AGPL subprocess tool). academic-research-skills excluded (CC-BY-NC). Process lessons: long multi-line patch strings mangle in transit on this host — short unique anchors + re-read on match failure; large write_file payloads (>~5KB) can be SILENTLY TRUNCATED (13KB landed as 262 bytes) — copy from on-disk source or verify sha256 after big writes; multi-point doc count updates go through a verification script asserting each anchor occurs exactly once; gen-skills-index.py regenerates all 23 category DESCRIPTIONs (re-run after any skill add); README xref + REFERENCES-INDEX counts hand-maintained — reconcile vs generator output each batch. OPEN: root AGENTS.md drafted, write gated on user approval.
§
Hermes_Skills ROUND-7a+7b COMPLETE -> CURRENT HEAD b5f0ea1 (supersedes "3d508a9"/"102557e"; local==remote, tree clean, CI green @ b5f0ea1). 7a = cross-link enrichment: isolated nodes 17->1, xrefs 425->449. 7b = first new test suite (devops/sqlite-queries/tests/ — 20 stdlib-only tests; 4 CLI tests run in CI only since ubuntu ships sqlite3; local binary for repro = official sqlite-tools-win-x64 zip from sqlite.org/<year>/) + run-skill-tests.py now names failing tests on failure. CRITICAL FIX: regen-dependency-map.py (+ built-in copy inside sync-hermes-skills.py) embedded "*Last generated: <today>*" in DEPENDENCY.md -> drift gate failed in CI for ANY commit crossing UTC midnight (local passed because local date == commit date); stamp removed from both generators — NEVER embed wall-clock dates into regenerated index files. Re-scan 2026-09-10: upstream optional-skills still 140, ECC still 286 skills — no new content since round-6 sweeps (baseline lists saved in temp star-scan3/). Still 184 skills / 23 categories / 299 reference docs. Also unnoted earlier: commit 2608d82 added evolutionary-ml ga-tuning-measured ref + CR-pipeline-derived additions to verification-culture/python-craft.