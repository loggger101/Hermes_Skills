User works on the Hermes_Skills repo on Windows. Key gotchas: str(path) uses backslash separators so forward-slash substring filters (e.g. "profiles-export/") fail silently; python3 on Windows resolves to a broken Store stub (exit 49) — use shutil.which("python") first. The cronjob system runs on Windows (HERMES_HOME=C:\Users\Owner\AppData\Local\hermes) and the user always triple-checks: git tracking sync, stray directories (profiles-export/memories-export/.hermes runtime data), orphaned branches, audit thresholds, and local env parity.
§
User works on Hermes_Skills repo on Windows + aspirecures repo (weekly research feed pipeline with two-agent cronjob architecture). Always triple-checks: git sync, stray dirs, orphaned branches, audit thresholds, env parity. Prefers perfect/foolproof autonomous solutions. Triple-check expectation: after deliverables verify (1) git fully pushed & 0 ahead/behind, (2) working tree clean, (3) no temp files, (4) no accidental deletions, (5) audit passes.