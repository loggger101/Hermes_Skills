# Repo tooling (tools/*.py). pyyaml is a HARD dependency of audit-skills.py and
# regen-dependency-map.py -- without it the audit cannot validate any frontmatter and
# sync-hermes-skills.py's index-regeneration step (which shells out to the latter)
# would fail. Install with: pip install -r requirements.txt
pyyaml>=5.1

# NVIDIA AI Workbench environment (see .project/spec.yaml), not used by tools/
jupyterlab>3.0
