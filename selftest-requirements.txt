# Dependencies for the dedicated CI job that executes the skill self-test harnesses
# (tools/run-self-tests.py). The base verify-all job runs them too, but without these
# deps the data-science harnesses classify as SKIP; this job is what actually exercises
# duckdb/polars/pyarrow against their documented behavior on every push/PR.
#
# Pins match the versions named in the reference docs they verify:
#   big-data-patterns.md / python-data-science  -> duckdb 1.5.5, polars 1.44.1 (stable),
#                                                  pyarrow 25.0.1, pandas <3 (pandas 3.x
#          has breaking changes; the harness ground-truth comparisons were captured on 2.x)
#   algorithms-python-catalog                   -> numpy
#   optimization-modeling-pyomo                 -> pyomo 6.10.1 + highspy (HiGHS solver;
#          both ship cp312 manylinux wheels on PyPI with zero required deps beyond the
#          already-present numpy — verified via PyPI metadata, so CI install cannot drift
#          into a source build)
#
# polars-v2-verify.py pins POLARS 2.0 rc behavior and exits 77 (SKIP) under this stable
# install by design — it is exercised locally in a dedicated venv (see its docstring).

numpy>=1.26,<3
duckdb==1.5.5
polars==1.44.1
pyarrow==25.0.1
pandas>=2.2,<3
pyomo==6.10.1
highspy==1.15.1
